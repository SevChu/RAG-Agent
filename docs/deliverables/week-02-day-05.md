# 第 2 周计划日 5 验收说明：一致性与真实资料结构

## 交付结论

计划日 5 已完成并通过验收。五类资料均通过真实上传、后台解析、结构化分块、
BGE-M3 Embedding 和 Qdrant 写入；最终隔离验收库包含 5 份原文件、5 条 SQLite 记录和
711 个向量，自动一致性错误 0、提醒 0。用户已于 2026-08-03 确认肉眼抽查通过。

本计划日没有新增模型、依赖或下载，没有读取 DeepSeek Key。

## 请打开这两个文件

主要抽查报告：

```text
D:\Agentic\data\acceptance\week-02\structure-review.md
```

PDF 原页摘录：

```text
D:\Agentic\output\pdf\第二周验收-数据结构教材代表页.pdf
```

PDF 摘录来自原 355 页教材的连续第 99～105 页，摘录第 1～7 页对应原 PDF
第 99～105 页（书内印刷页 87～93）。为避免验收文件一开始就是上一小节的半页残段，
第 1 页只保留从“2. 后缀表达式”开始的区域；所选小节正文没有改写，第 2～7 页保持原页。
最终文件已重新逐页渲染，确认文字清晰、没有遮挡、错序或正文裁切。

## 建议抽查顺序

### 1. PDF / OCR

在结构报告找到 `01-课程教材-连续真实页摘录.pdf`，对照 PDF 看三个样本：

- Chunk 0 应来自摘录第 1 页，以 `2. 后缀表达式` 为章节路径，并完整覆盖该小节的定义、
  示例、公式和结论，不再包含上一小节残段；
- Chunk 6 应来自摘录第 4 页，包含表 4-3 及运算符优先级说明；
- Chunk 11 应来自摘录第 7 页，包含代码结尾和算法复杂度结论。

允许 OCR 有少量字符误识别；不能接受把页 1 的中缀/后缀内容与无关章节拼接，或把表格、
公式相关说明切得无法理解。

### 2. PPTX

报告均匀抽取幻灯片 1、67、134 附近的 Chunk。检查：

- 幻灯片编号与原课件一致；
- 首尾章节分别接近“数据结构2”和“图中算法”；
- 中部内容仍属于图/最短路径相关部分；
- 没有跨幻灯片混入其他课程资料。

### 3. Markdown

扩充后的样本包含 3,550 个正文字符、9 个 Chunk。抽取 Chunk 0、4、8，应该依次体现
总标题、栈与括号匹配代码、复习与验收问题。章节路径应为：

```text
数据结构课程学习笔记 / 对应小节
```

代码和问题列表应保持为完整单元。

### 4. DOCX

抽取 Chunk 0、6、13，应该覆盖“实验目的”“创建普通云盘”“实验结果 / 创建快照”。
检查三级章节路径是否对应原文，并确认图片占位没有跑到无关章节。

### 5. TXT

TXT 样本是英文长文本压力样本，不是数据结构教材。纯文本解析现在会保守识别独立的
`Book`、罗马数字和短标题，并在没有足够空间容纳完整重叠句时放弃该段重叠，不再让下一个
Chunk 从句中开始。报告抽取 Chunk 0、269、538；中部样本应带正确的 Book/Chapter 路径，
正文应从完整句子开始，且不能把年份开头的普通句子误判为标题。

## 人工验收标准与结果

用户已按以下四点完成抽查并确认计划日 5 通过：

1. 文件名、类型和课程归属正确；
2. Markdown、DOCX、PPTX 的章节或幻灯片位置与原资料大致一致；
3. PDF 页码对应，公式、表格、代码与相关说明基本保持在可理解的 Chunk 中；
4. 开头、中部和末尾样本没有明显跨章节拼接或语义突然中断。

本次验收不要求逐字检查 711 个 Chunk，也不要求人工打开 SQLite 或 Qdrant；抽查中未报告
阻塞第三周的结构问题。

## 自动验证摘要

| 检查 | 结果 |
|---|---|
| 五类资料最终状态 | 5/5 completed |
| SQLite / 原文件 / Qdrant | 5 / 5 / 711，一致 |
| 审计错误 / 提醒 | 0 / 0 |
| 同课程重复 | 409，符合规则 |
| 不支持格式 | 415，符合规则 |
| 损坏 PDF | failed，中文提示，无残留向量 |
| 跨课程相同内容 | 允许，课程隔离正确 |
| 临时课程删除 | 文件、记录、向量全部清理 |
| 重新索引 | 完成且没有旧向量叠加 |
| 后端 pytest | 94 项通过 |
| 前端 Vitest | 8 项通过 |
| Ruff / Mypy / Lint / TypeScript / Build | 全部通过 |
| npm audit | 0 个已知漏洞 |

## 验收数据说明

隔离验收库暂时保留在 `data/acceptance/week-02/`，便于你抽查；它没有进入正式
`data/app.db` 或正式 Qdrant Collection。正式库已单独审计：0 资料、0 上传文件、0 向量、
0 一致性错误。

按每日验收后提交的约定，本计划日暂未提交 Git；等你确认肉眼抽查后再提交。
