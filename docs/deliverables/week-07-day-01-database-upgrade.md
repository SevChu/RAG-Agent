# 第七周 Day 1：正式数据库备份与升级

- 执行日期：2026-09-08。
- 用户已授权备份和正式数据库升级，并要求评估旧数据能否删除。
- 结果：20260810_04 → 20260907_05，正式升级及数据核验完成。
- 软件版本仍为 1.2.1；迁移版本与软件 Release 版本独立。

## 执行过程

1. 核对实际数据库为 data/app.db，uploads 和 Qdrant 使用项目默认目录。
2. 未发现本项目后端进程或 8000 端口监听；升级前再次检查并验证 SQLite 可取得独占事务锁。
3. 在仓库外创建完整业务备份，使用 SQLite backup API 保存数据库，复制 uploads 和 Qdrant；
   为数据库与各资源文件生成 SHA-256 清单，执行完整性、外键和内容校验。
4. 在隔离数据库副本上执行真实数据升级演练，Alembic check 无结构差异；全部旧表内容哈希一致。
5. 再次确认正式库与资料资源未变化，执行正式升级并核验。
6. 用 mode=ro 的 SQLite 连接验证新版 ORM 可读取历史会话及关联消息。

备份标识：week07-pre-20260907_05-20260908-100311。
备份业务文件共 84,259,992 bytes（约 80.4 MiB），包括完整 SQLite、3 个上传文件和 3 个向量库文件。
备份 manifest 和 upgrade-result 仅保存在私有备份目录；演练数据库和操作状态位于被 Git 忽略的 tmp。
不在公开报告保存资料名称、样本 ID、聊天正文或逐行哈希。

## 核验结果

| 内容 | 升级前 | 升级后 |
|---|---:|---:|
| 资料空间 | 2 | 2 |
| 资料记录 | 3 | 3 |
| 会话 | 11 | 11 |
| 消息 | 28 | 28 |
| Token 使用事件 | 10 | 10 |
| 智能体身份 / revision | 无对应表 | 0 / 0 |

- 不仅行数一致，五张旧业务表全部原有列的内容哈希也一致，包含引用、诊断和消息内容。
- 旧会话的新绑定字段均为 NULL，没有伪造回填或历史重绑定。
- SQLite integrity_check=ok，foreign_key_check 无错误。
- Alembic check 无新增待执行结构变更，5 个保护触发器全部存在。
- 无 _alembic_tmp 残留表；原 conversations 是升级后的正式表。
- uploads 与 Qdrant 文件清单/哈希均未变化，备份数据库哈希保持不变。
- 只读 ORM 读取全部 11 个会话和关联的 28 条消息通过。
- 原后端处于停止状态，本次未额外启动服务或调用真实模型。

## 旧数据清理评估

| 对象 | 结论 |
|---|---|
| 正式 app.db 及旧会话、消息、资料记录 | 保留；升级沿用原业务数据，不是无用旧副本 |
| uploads、Qdrant | 保留；现有资料与检索仍依赖它们 |
| 旧 Alembic 迁移脚本 | 保留；全新安装、升级链与回退仍需要 |
| 本次完整备份 | 建议至少保留到 v1.3.0 验收完成，再按备份保留策略处理 |
| tmp/week07-day01-focused、focused-final、full、final | 已完成且被 Git 忽略的测试产物，可删除；合计 20,216,754 bytes |
| 本次 tmp 中的 rehearsal.db | 演练完成，可删除；634,880 bytes |
| Benchmark 数据、模型及冻结实验输出 | 本次迁移没有使它们失效，不列为清理对象 |

上述可清理测试产物与演练数据库合计 20,851,634 bytes（约 19.9 MiB）。本次只评估，未执行删除。
删除任何备份、资料或历史记录都不能以“版本旧”作为唯一依据。

## 后续状态

当前正式库已经满足 Day 1 ORM 的字段要求，无须重复迁移本版本。
后续 Day 2/3 开发按[周计划](week-07-plan.md)继续；没有管理 API 或运行配置接入的新增完成声明。
升级没有新增依赖、下载模型、运行 Benchmark、提交 Git 或发布 Release。
