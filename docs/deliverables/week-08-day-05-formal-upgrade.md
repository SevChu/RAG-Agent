# 第八周 Day 5：正式数据库升级与 worker 启用记录

- 执行日期：2026-09-13。
- 授权：用户明确“批准以上操作”，批准范围为[审查单](week-08-day-05.md) A～C。
- 状态：正式备份、迁移、服务恢复与一组合成冒烟验收完成；服务保持运行。
- Day 5 的候选版本收尾、周总结与发布材料仍待后续，不将本次操作记为整个 Day 5 或 beta 发布完成。

## 执行结果

| 项目 | 实际结果 |
|---|---|
| 数据库 | D:/Agentic/data/app.db，20260910_06 → **20260911_09** |
| 迁移检查 | Alembic check 无差异；SQLite 完整性和外键通过；28 个触发器；无临时重建表残留 |
| 原业务数据 | **原 48 条记录与既有结构保持**；7 个资料会话和 2 个快速对话的原 ID 均可读取 |
| 原资源 | uploads 与 Qdrant 文件集合和 SHA-256 未变；既有模型目录未操作 |
| 配置 | 根 .env 仅追加 TRAINING_WORKER_ENABLED=true；原有字节、凭据、模型配置和路径保留 |
| 索引 | 临时核验进程关闭自动索引；正式进程恢复原 auto_index_documents=true，3 份文档均 completed |
| worker | 正式启用，实际领取并完成任务；最终无待执行任务，租约处于空闲状态 |
| 浏览器 | 正式页面可见数据审核、五步提交、成功/刷新、Adapter 来源；最终页面错误 0 |
| 应用源码与依赖 | 应用代码、锁文件与准备阶段已验证指纹一致；没有安装或更新依赖 |

## 备份与恢复证据

正式执行前重新检查了库/配置/代码/资源指纹及服务状态，无活动 app.main:app 进程；未停止无关 Python/Node 进程。

新备份位于 `D:/Agentic/data/backups/week08-pre-20260911_09-20260913-105720/`，没有覆盖已有备份。数据、配置与代码包共 **9 个文件、86,288,084 bytes**（另含备份清单/记录）；包括 SQLite backup、3 个 uploads 文件、3 个 Qdrant 文件、根配置与科研审阅源码包。backend/.env 不存在，已记录；训练目录执行前不存在。

备份文件逐一校验 SHA-256，数据库完整性/外键通过。另建独立恢复校验副本，确认 06 版本、原结构与原记录一致。最终审计再次核对备份未变。完整清单和配置仅留 Git 忽略的私有备份，不随报告分发。

备份在同一本地磁盘，用于本次变更恢复；没有进行覆盖恢复、正式降级或删除。若日后需回到 06，应先保存当前 09 数据和训练文件，审查备份后新增记录的损失范围，再执行具体恢复。

## 分阶段执行

1. 重新备份及恢复副本核对通过后，使用现有 Python/Alembic 固定升级到 09；迁移和核验约 0.67 秒（仅本机本次执行）。
2. 迁移后先核对原 48 条数据、7 张新表为空、结构和资源指纹。在 worker=false、自动索引=false 的核验进程中读取健康、智能体、空间、两类会话及训练空列表。
3. 核验进程优雅退出后，仅追加 worker=true 配置。确认无 pending/processing 文档，再启动恢复原索引配置的正式后端。
4. 启动本地前端，复用现有依赖，连接 `http://127.0.0.1:8000/api`。登记并批准独立合成数据，通过五步页面提交唯一任务，再验证后台执行、刷新及产物。

## 唯一一组合成验收记录

专用智能体名称为“Week8 正式冒烟（合成）”，数据集为“Week8 正式冒烟（合成）数据”。新增首个智能体 revision、一个数据版本、3 条审核事件（草稿/待审核/批准），仅含 train/validation 各 1 条独立水果/天体合成文本。未修改原智能体，未使用真实文档或会话作为训练数据。

- 任务：`844bbbd1-b72a-43c1-beb1-b2854352288d`，3/3 步，succeeded。
- 事件：6 条，序号连续；包含后台领取/运行与成功。
- Adapter：`c631f11b-b0cf-59a8-b545-98d877be4185`，integrity=valid、deployable=false。
- 训练目录最终仅有一个 JSONL 和一个模拟 manifest，共 **2,181 bytes**。
- 当前总量：智能体 2、智能体版本 3；原会话 9、消息 26、空间 2、文档 3、Token 事件 5 保持；训练任务 1、产物 1。

保留这些明确标识的正式验收记录供用户查看，没有从此前演练目录导入旧任务或产物，也没有自动清理记录。

[正式合成 Adapter 页面截图](assets/week-08-day-05/formal-synthetic-adapter.png)仅截取合成产物区域，不包含真实会话侧栏或凭据。

## 当前入口与服务管理

- 页面：[微调实验室](http://127.0.0.1:5173/training/datasets)。
- 任务：[本次成功任务](http://127.0.0.1:5173/training/runs/844bbbd1-b72a-43c1-beb1-b2854352288d)。
- 产物：[本次 Adapter](http://127.0.0.1:5173/training/adapters/c631f11b-b0cf-59a8-b545-98d877be4185)。
- 后端：[健康检查](http://127.0.0.1:8000/api/health)。

本次正式后端 PID **27792**，前端 PID **4280**，均监听 127.0.0.1。它们是本次启动的本地进程，没有注册开机自启服务。PID 仅对本次启动有效，后续操作应重新核对身份。

后端由 `tmp/week08-day05/formal/serve.py on` 启动，仍加载同一 app.main:app 和正式 .env；该启动器只加入受控的优雅停止文件监视。需要停止时，在确认本实例和任务状态后创建 `tmp/week08-day05/formal/stop-on`，等待日志出现 Application shutdown complete；不批量结束 Python 进程，不清理仍在使用的 tmp 控制/审计文件。

将来普通重启可在 backend 目录运行现有 `.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`，它会读取已保存的 worker=true；先确认旧端口已释放。前端在 frontend 目录按原流程启动；若显式设置 VITE_API_BASE_URL，必须包含 `/api`。本次未向根 .env 写入前端地址。

## 执行中发现并处理的问题

- 初始核验脚本把 `/conversations` 当作全部会话接口，期望 9 条；该接口实际只列资料会话。读取路由并逐 ID 核对后，改为资料 7 + 快速 2，确认无数据丢失。未改变应用业务逻辑。
- 本次前端启动参数遗漏 `/api` 导致 options 等请求 404；修正该进程的 API 基地址并重启前端，根配置未新增无关键。同步纠正 Day 4 复现文档的两处基地址说明。
- 任务完成后“模拟成功”同时出现于状态标识和事件表，验收脚本的严格定位产生多匹配；改为定位状态标识并只读复核。该任务没有重提或重复执行。
- CUA/托管 Node 控制会话初始化失败，使用已有 Playwright CLI 与已安装 Chromium 完成浏览器验证；无新增软件。

最终浏览器复核 4 组检查通过，页面错误 0；五步提交已在此前同一任务创建环节完成，复核脚本复用该任务继续验证，不将其计为第二个任务。

## 边界与后续

实际证据位于 `tmp/week08-day05/formal/`，包含备份/迁移/关闭 worker 核验/单键变更/合成任务/最终审计及启动日志。准备阶段全量回归仍为 519 passed、1 skipped，前端 37 passed，产品包 80 passed；本次没有修改应用源码，因此未重复全部单元测试，增加的是已批准的正式环境验证。

本次未执行真实训练、模型请求、下载、Adapter 部署、删除或数据库恢复，也未提交、Tag、推送或发布。运行版本仍为 1.3.0；v1.4.0-beta.1 的元数据、Release Notes、周总结与发布清单另行收尾，不把模拟产物当作真实模型能力。


## 发布准备后的版本说明

本报告记录正式迁移当天的执行结果、服务身份和备份，保留当时 1.3.0 运行版本事实。
随后候选源码已准备为 v1.4.0-beta.1；本轮未重启上述正式进程或重新迁移。发布审阅与候选验证见
[审批单](../releases/v1.4.0-beta.1-approval.md)，PID 是否仍存活应以实际运行状态为准。
