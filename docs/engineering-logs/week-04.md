# 第 4 周工程日志：Agent、总结与出题

## 周信息

| 项目 | 内容 |
|---|---|
| 周主题 | Agent、总结与出题 |
| 当前计划日 | 计划日 1：混合来源基础（已完成并通过用户验收） |
| 当前状态 | 计划日 1 已收口；LangGraph 混合来源编排待计划日 2 |
| API Key | 复用根目录 `.env` 的 `LLM_API_KEY`；未输出、未记录、未提交 |
| 数据库迁移 | 无 |

## 已确认约束

- 问答默认范围为“课程资料 + 外部补充”，同时保留“仅课程资料”；
- 混合模式先检索课程资料，只在覆盖不足、明确要求外部内容或问题具有时效性时搜索；
- 课程与外部来源分别使用 `[课n]`、`[外n]`，不共享编号空间；
- 外部来源优先论文、大学、科研机构、标准组织和官方文档；
- 模型记忆不能伪装成外部来源，外部事实必须对应真实搜索结果 URL；
- Web Search 失败时不能拖垮课程回答，具体降级编排在计划日 2 实施；
- 总结默认仅课程资料并包含考试重点；出题默认 20 题、默认偏向 C++，课程改编题不超过
  30%，外部补充题不超过 20%。

## 计划日 1：混合来源基础

### 完成内容

#### 请求、响应与持久化契约

- 新增 `AnswerScope`：`course_and_external`（默认）与 `course_only`；
- 新增 `CitationSourceType`：`course` 与 `external`；
- 引用响应预留外部标题、发布方、URL、访问时间与证据摘要字段；
- 新增外部搜索状态：未请求、成功、无合格结果、失败；
- 回答响应和会话检索快照记录回答范围及搜索状态，旧会话默认解释为课程范围；
- 课程正文引用从旧 `[n]` 规范化为 `[课n]`，仍兼容模型偶发输出旧格式。

#### 外部搜索适配器

- 使用现有 Key 调用 DeepSeek Anthropic 兼容 `/anthropic/v1/messages`；
- 使用服务端 `web_search_20250305` 工具，限制每次搜索次数和保留结果数；
- 直接解析 `web_search_tool_result` 返回的真实标题和 URL；
- 原始搜索正文是加密块，适配器不会把它伪装成原文片段；同一次响应生成证据摘要，只有摘要
  URL 能与真实结果 URL 对齐时才接纳；
- 清除常见跟踪参数、按 URL 去重，过滤论坛、自媒体和聚合站点；
- 按学术、机构、官方、专业来源排序；失败返回结构化状态，不抛出密钥或响应正文。

#### 前端基础

- 课程助手增加回答范围选择器，默认“课程资料 + 外部补充”；
- 清楚标注计划日 1 尚未把外部搜索接入答案链路，避免误导；
- 课程与外部引用采用不同编号与卡片颜色，外部卡片预留发布方、URL 和访问时间；
- 回答底部显示请求范围与外部搜索状态。

### 真实 API 验证

- 现有 Key 成功触发 `server_tool_use` 与 `web_search_tool_result`；
- 正常样本返回 10 条原始搜索结果，适配器保留 4 条 URL 可核验来源；
- 结果包含 Python 官方文档、Python Software Foundation 发布页和 CPython 官方仓库；
- 验证期间观察到一次 HTTP 503，适配器按失败状态返回；再次请求成功，说明失败路径可观测，
  计划日 2 可据此执行课程回答降级。

### 自动验证

- 后端：143 项测试通过；
- 后端静态检查：Ruff、Mypy strict 通过；
- 前端：10 项单元测试、Vue TypeScript、生产构建通过；
- 真实 Web Search：成功；
- 无数据库迁移、无新依赖、无模型下载。

### 计划日边界

本日没有实现 LangGraph 条件路由、课程覆盖判断、混合答案生成、`[外n]` 正文校验、来源冲突
说明或搜索失败后的课程答案降级。这些属于计划日 2，验收计划日 1 时不应要求外部来源已经
出现在课程回答中。

### 用户验收结果

用户于 2026-08-04 完成计划日 1 验收，并确认“验收通过，基本上没问题”。计划日 1 据此
正式收口；后续改动从计划日 2 的 LangGraph 条件搜索、混合生成、引用校验与失败降级开始。
